version: "3.9"
services:
  readbrain:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./books:/app/books
      - ./site:/app/site
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
